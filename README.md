# Badge Wall (built output)

This repository holds only the compiled static site, published by CI so
GitHub Pages can serve it for free. Nothing here is hand-edited and the
history is replaced on every deploy.

Live at https://filaipus.github.io/badge-wall/
